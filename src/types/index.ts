export interface Video {
  id: string;
  title: string;
  channelName: string;
  views: string;
  time: string;
  duration: string;
  thumbnail: string;
  avatar: string;
}

export interface Chip {
  text: string;
  isActive: boolean;
}
